let testInput = "1000
2000
3000

4000

5000
6000

7000
8000
9000

10000"

(* let (>>) f g x = g (f x)

(* let parse = String.split_on_char '\n' >> List.map Belt.Int.fromString >> Belt.List.keepMap (Belt.Option.mapWithDefault 0 (fun x -> x)) *)
let parse = String.

let testing123 = parse testInput *)